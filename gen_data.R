# generate data

# set.seed(0)
# m <- runif(100, min=.6, max=6.4)
# d <- x*.5 + .5*runif(10, min=.6, max=5.4)
# e <- x*.3 + .7*runif(10, min=.6, max=5.4)
# j <- runif(100, min=.6, max=3.4)
# p <- x*.5 + .5*runif(10, min=.6, max=5.4)
# d1 <- data.frame(jahrgang=round(j, 0), partei=round(p, 0),
#                  mathe=round(m, 0), deutsch=round(d, 0), englisch=round(e, 0))
# write.table(d1, "ryouready/data/d1.txt")

